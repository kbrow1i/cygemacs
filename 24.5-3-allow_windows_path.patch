--- origsrc/emacs-24.5/src/fileio.c	2015-04-02 03:23:06.000000000 -0400
+++ src/emacs-24.5/src/fileio.c	2016-01-23 13:29:33.894089400 -0500
@@ -80,6 +80,11 @@ along with GNU Emacs.  If not, see <http
 #define DRIVE_LETTER(x) c_tolower (x)
 #endif
 
+#ifdef CYGWIN
+#define IS_ABSOLUTE_WINDOWS_PATH(x) (c_isalpha (x[0]) && (x[1] == ':') \
+				     && (x[2] == '/' || x[2] == '\\'))
+#endif
+
 #include "systime.h"
 #include <acl.h>
 #include <allocator.h>
@@ -1017,6 +1022,14 @@ filesystem tree, not (expand-file-name "
   /* Make a local copy of nm[] to protect it from GC in DECODE_FILE below.  */
   nm = xlispstrdupa (name);
 
+#ifdef CYGWIN
+  if (IS_ABSOLUTE_WINDOWS_PATH (nm))
+    {
+      name = Fcygwin_convert_file_name_from_windows (name, Qt);
+      nm = xlispstrdupa (name);
+    }
+#endif
+
 #ifdef DOS_NT
   /* Note if special escape prefix is present, but remove for now.  */
   if (nm[0] == '/' && nm[1] == ':')
@@ -1691,6 +1704,9 @@ file_name_absolute_p (const char *filena
      || (IS_DRIVE (*filename) && IS_DEVICE_SEP (filename[1])
 	 && IS_DIRECTORY_SEP (filename[2]))
 #endif
+#ifdef CYGWIN
+     || IS_ABSOLUTE_WINDOWS_PATH (filename)
+#endif
      );
 }
 
