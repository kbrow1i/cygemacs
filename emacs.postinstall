#!/bin/sh

prefix=/usr
bindir=${prefix}/bin
sbindir=${prefix}/sbin

${sbindir}/update-alternatives \
	--install ${bindir}/emacs emacs ${bindir}/emacs-nox.exe 10
${sbindir}/update-alternatives \
	--install ${bindir}/emacsclient emacsclient ${bindir}/emacsclient-nox.exe 10
