--- origsrc/emacs-24.3.93/lisp/startup.el	2014-07-10 12:00:14.000000000 -0400
+++ src/emacs-24.3.93/lisp/startup.el	2014-08-30 08:23:09.063579500 -0400
@@ -1812,7 +1812,7 @@ we put it on this frame."
   (let (chosen-frame)
     ;; MS-Windows needs this to have a chance to make the initial
     ;; frame visible.
-    (if (eq system-type 'windows-nt)
+    (if (eq (window-system) 'w32)
 	(sit-for 0 t))
     (dolist (frame (append (frame-list) (list (selected-frame))))
       (if (and (frame-visible-p frame)
